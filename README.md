# Mock server for Breaking API

Mock server to replicate the responses made by the Breaking API Services

## Set up

1. Installation

    ```bash
    npm install
    ```

2. Run server

    ```bash
    node mockserver.js
    ```
